
spring.datasource.url = jdbc:mysql://localhost:3306/oauth2?useUnicode=yes&characterEncoding=UTF-8
spring.datasource.username = root
spring.datasource.password = 

# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle = true
spring.datasource.validationQuery = SELECT 1


# Show or not log for each sql query
#spring.jpa.show-sql = true

#Application specific
authentication.oauth.clientid=testapp
authentication.oauth.secret=secret
authentication.oauth.tokenValidityInSeconds=1800
